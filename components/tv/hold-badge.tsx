export function HoldBadge() {
  return (
    <span className="inline-flex items-center gap-1.5 rounded-md bg-status-orange/10 border border-status-orange/20 px-2 py-0.5 text-[10px] font-semibold uppercase tracking-[0.08em] text-status-orange">
      <span className="h-1 w-1 rounded-full bg-status-orange" />
      Hold
    </span>
  );
}
